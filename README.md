# LibraryManagementSystem
A simple Library Management System built in Java (JDBC + MySQL). This project allows the admin to manage books, issue/return books, and automatically calculate fines using SQL.
